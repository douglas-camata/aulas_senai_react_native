import { StyleSheet } from "react-native";

const estilo1 = StyleSheet.create({
    texto : {
        fontSize: 20,
    }
})

const estilo2 = StyleSheet.create({
    texto2 : {
        color: 'red',
    }
})

export {estilo1, estilo2}